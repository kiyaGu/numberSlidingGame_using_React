import React, { Component } from "react";
import { Button } from "reactstrap";
export const ResetGameButton = () => {
  return (
    <Button className="btn-md" outline color="secondary" id="reset">
      Reset
    </Button>
  );
};
