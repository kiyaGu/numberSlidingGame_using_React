import React from "react";
import { Button } from "reactstrap";
export const ResetGameButton = () => {
  return (
    <Button className="btn-block" outline color="secondary" id="reset">
      Reset
    </Button>
  );
};
